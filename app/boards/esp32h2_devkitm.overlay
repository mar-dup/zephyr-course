/ {
    leds {
        compatible = "gpio-leds";

        my_led: my_led {
            gpios = <&gpio0 0 GPIO_ACTIVE_LOW>;
            label = "My Custom LED";
        };
    };

    aliases {
        led0 = &my_led;
    };
};